# Runtime dependencies — exact versions from the verified working environment
# (FIX-20), so an install is reproducible instead of "whatever satisfies the range
# today". After a deliberate upgrade run `pip freeze` and re-pin the package here.
aiogram==3.31.0
fastapi==0.141.1
uvicorn==0.53.0
SQLAlchemy[asyncio]==2.0.52
aiosqlite==0.22.1
alembic==1.20.0
httpx==0.28.1
pydantic==2.13.5
pydantic-settings==2.15.0
phonenumbers==8.13.55
rapidfuzz==3.14.6
gspread==6.2.1
google-auth==2.58.0
